<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hackathon.recumeet">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:name=".Recumeet"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Recumeet">
        <activity android:name=".chat.UsersActivity"></activity>
        <activity android:name=".chat.ChatActivity" />
        <activity android:name=".UserProfile.ProfilePosts"></activity>
        <activity android:name=".UserProfile.EditProfile2Activity" />
        <activity android:name=".UserProfile.ConnectionActivity" />
        <activity android:name=".Feed.AddPostActivity" />
        <activity android:name=".Feed.LikeCommentActivity" />
        <activity android:name=".UserProfile.ProfileActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".UserAuth.CreateUserActivity" />
        <activity android:name=".UserAuth.ForgetPasswordActivity" />
        <activity android:name=".UserAuth.RegisterActivity" />
        <activity android:name=".UserAuth.LoginActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>